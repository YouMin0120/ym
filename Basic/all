#n=[숫자와 문자를 여러가지 넣을수 있음]
#n.append(리스트에 들어가는 문자 또는 숫자를 추가할수 있음)
# len(길이를 구하는 함수)
# random.randrange 를 실행시키기 위해서 사용하는 함수 = import 이 import 는 코드의 처음에 넣어야한다
# 함수를 def + 함수이름(): 으로 만들수 있다.
# 함수 return 을 사용하기 위해선 def test(): 에서 생성된 함수 값을 a=() 와 같은 함수로 지정해서 사용해야한다.
# 클래스(class)는 프로그램이 실행되었을 때 생성되는 개체가 어떤 변수를 가지는지 정의해둔 것을 말합니다 / class와 __inhit__은 같이 사용된다
